# Next.js on Netlify — use Node 20 LTS (exact patch like 22.21.1 may be missing on build image)
[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit --no-fund"
